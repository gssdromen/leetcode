#include<iostream>
#include<vector>
using namespace std;


class Solution {
public:
    int minimumTotal(vector<vector<int> > &triangle) {
        for(vector<vector<int>>::iterator iter=&triangle.begin();
            iter !=&triangle.end(); iter++; ){

        }
    }
};

int main(void){
    Solution *ss = new Solution();
    // ss->minimumTotal[[1]];
    // vector<string> result = ss->hasPathSum(6);
    // cout<<ss->result[0];
    // for(vector<string>::iterator it=result.begin();it!=result.end();it++){
    //     cout<<*it<<endl;
    // }
    return 0;
}
