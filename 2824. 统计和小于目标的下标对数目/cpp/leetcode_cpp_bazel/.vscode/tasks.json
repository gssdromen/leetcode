{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "bazel build",
      "type": "shell",
      "command": "bazel build //src:main --compilation_mode=dbg --spawn_strategy=standalone --strip=never",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
